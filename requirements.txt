PySide6>=6.6
openpyxl>=3.1
Send2Trash>=1.8
rapidfuzz>=3.0
py7zr>=1.1.3
keyring>=24
# Local star-rating / difficulty computation (v1.5). Compiled Rust wheel; pinned
# for reproducibility. OPTIONAL at runtime — rosu.ratings import-guards it, so the
# app still runs (without local stars) if the wheel is missing.
rosu-pp-py==4.0.2
