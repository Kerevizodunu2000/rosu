# Working data folders (never commit the user's music/archives)
/Packs/
/Output/
/Library/
/Processed/
/data/
/logs/
config.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# PyInstaller build output
/build/
/dist/
*.spec.bak

# Built executable (~94MB; distributed via GitHub Releases, not the repo)
/rosu.exe
/osu-archiver.exe
*.exe
# ...but DO keep the bundled lazer-export helper (a build input for rosu.exe)
!rosu/assets/lazer_export/RosuLazerExport.exe

# .NET build output for the lazer helper
tools/RosuLazerExport/bin/
tools/RosuLazerExport/obj/
tools/RosuLazerExport/FodyWeavers.xml

# Internal design docs, specs, icon-design galleries, and handoff notes —
# not for the public repo (they also contain local absolute paths).
HANDOFF.md
docs/
